1. 파이썬의 기본 데이터 타입 4가지
정수, 실수, 논리, 문자열
2. 변수, 변수명 비교 변수명에 들어가는 것이 변수
3. 다음 코드를 설명하세요.(cpu와 ram 관계 중심으로)
start = 100        ram에서 cpu가 start라는 memory를 만들어 값 100 저장
sum = start + 200  ram에서 cpu가 start+200를 계산해 ram에 sum이라는 memory를 만들어 저장
4. 많은 양의 데이터를 ram에 만드는 방법 중 순서가 있는 목록을 만드는 방법
list를 사용
5. 리스트에서 index란? index의 시작값은? 리스트의 개수 세는 방법은?
index란 []안의 몇번째 위치, 0, len함수
6. range(0,5,1)로 생성되는 범위를 쓰시오.
[0,1,2,3,4]
7. *을 3번 프린트하는 코드를 완성해보세요 (for문)
for i in range (0,3,1):
    print(*)
8. while True:
    print('나는 참이야')
    의 의미는?
    '나는 참이야'를 True인동안 반복하라 -> 무한반복
9. for x in [1,2,3,4,]:
    print(x)
    의 결과는?
    x
    x
    x
    x
10. 입력한 값을 정수로 변환해서 사용하고자 하는 경우, 실수로 변환해서 사용하고자 하는 함수
int(input()), float(input())
11. 아래와 같이 하는 이유는?
data = input('키를 입력하세요')
height = int(data)
print("내 내년 키는", height +10)
같은 형태끼리만 + 할 수 있기 때문에 앞에는 , 뒤엔 + 가 가능
12. 다음 문제는 어떻게 풀어야 할까요?
C1 = [22,99,11,23]
C2 = [44,99,66,23]
위 2반에서 같은 위치에 같은 값을 가지는 요소는 몇개이며, 해당 index와 값은 무엇인가?
count = 0
for x in [0,1,2,3]
    if C1[x]==C2[x]:
        count += 1
        print("index=",x,"값=",C1[x]

13. 리스트에서 인덱싱과 슬리이싱의 의미는?
인덱싱은 인덱스를 가져오는 과정, 슬라이싱은 인덱스와 함께 값을 가져오는 과정이다

14. 파이썬의 연산자 중 산술, 결합, 비교, 논리 연산자를 정리하고, 예를 들어보세요
산술 = 사칙연산 , %, //
결합 = 데이터프레임을 합침 "안녕"+"하세요"
비교 = ==, <, <=, >, >=, !=
논리 = and, or, if,

15. 파이썬에서 + 연산자를 쓸 때 주의점은?
같은 형태끼리만 +를 쓸 수 있다

16. 논리형 데이터에 들어갈 수 있는 값 2가지는?
True, False

17. 리스트에 요소를 추가하고, 삭제하는 방법은?
append, remove

18. IDE는 무엇의 약자? 무엇인가? 사용해본 IDE를 써보세요
Integrated Development Environement, 통합개발환경으로, 공통된 개발자 툴을 하나의 그래픽 사용자 인터페이스(Graphical User Interface, GUI)로 결합하는 애플리케이션을 구축하기 위한 소프트웨어입니다.
Eclipse IDE, pyCharm

19. Google colab서비스의 운영체제는 무엇이고, 어떤 서비스인가?
운영체제는 linux고 window에서 linux를 쓸 수 있게 해주는 서비스이다

20. 협업툴 사용해본 것은?
slack, notion, googledoc sheet
